{
  "name": "coldforms-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "live-server src --port=8080 --host=127.0.0.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
