# ColdForms Website

Marketing website for ColdForms, deployed automatically to GitHub Pages.

## Local preview

```powershell
npm install
npm start
```

The site source is in `src`. Every push to `main` deploys that folder through
the GitHub Pages workflow.
